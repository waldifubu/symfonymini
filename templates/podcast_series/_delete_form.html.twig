<form method="post" action="{{ path('app_podcast_series_delete', {'id': podcast_series.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ podcast_series.id) }}">
    <button class="btn">Delete</button>
</form>
